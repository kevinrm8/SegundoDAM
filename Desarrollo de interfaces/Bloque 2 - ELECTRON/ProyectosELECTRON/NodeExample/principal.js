// to use a module you need a require:
const utilities = require('./Utilities');

// Now we can use the functions defined in the module:
console.log(utilities.Add(3, 2));
console.log(utilities.Subtract(3,1));